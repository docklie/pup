# brought to you by puppet
[main]
    logdir   = /var/log/puppet
    rundir   = /var/run/puppet
    ssldir   = $vardir/ssl
    server   = puppet.london.maxeler.com
#    pluginsync = true
#    pluginsource = puppet://$server/plugins 


[agent]
    classfile = $vardir/classes.txt
    localconfig = $vardir/localconfig
    report = true
    listen = true
#    runinterval = 604800
